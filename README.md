# 📱 MTC Cloud (Private Project)

Приложение для визуализации и управления облачным хранилищем MTC Cloud. Проект предназначен для внутреннего использования.

---

## 🛠 Техническая реализация

Приложение построено на **Flutter** с использованием компонентного подхода и разделения на слои (Core/Features).

### 🎨 Интерфейс и Адаптивность

- **Responsive Design**: Использование `MediaQuery` для динамического расчета размеров иконок, текста и отступов. Приложение поддерживает апскейлинг и корректно отображается как на маленьких телефонах, так и на планшетах/десктопе.
- **Custom UI Components**: Полностью кастомный `BottomNavigationBar` на 5 элементов с вынесенной логикой переключения страниц через `IndexedStack` (сохраняет состояние страниц при переключении).

### 🌍 Локализация (Localization)

- **Система**: Использован `flutter_localizations` с кастомным классом `AppLocalizations`.
- **Управление состоянием**: Глобальный `LocaleProvider` (на базе `ChangeNotifier`) позволяет менять язык (RU/EN) в любой момент без перезапуска приложения.
- **Интеграция**: Все строковые ресурсы вынесены в карты локализации, доступ к которым осуществляется через `context`.

### 📂 Работа с файлами и медиа

- **Camera/Gallery**: Интеграция `image_picker` для захвата фото и выбора медиа из системной галереи.
- **File System**: Использование `file_picker` для доступа к документам и любым файлам в памяти устройства.
- **Context Safety**: Реализована логика безопасного извлечения `Navigator` до асинхронных вызовов (await), что предотвращает краши при возврате из внешних приложений (камеры/галереи) на новых версиях Android (API 34-36).

---

## 🚀 Настройка и запуск

### 1. Требования

- Flutter SDK (stable channel)
- Android SDK (с установленным API 34 для сборки)
- Подключенное устройство или эмулятор

### 2. Сборка

1. Загрузка зависимостей:
   ```bash
   flutter pub get
   ```
2. Запуск проекта:
   ```bash
   flutter run
   ```

---

## 🏗 Архитектура проекта

- `lib/core/`: Общая логика — система локализации (`l10n`), темы (`theme`) и глобальные виджеты.
- `lib/features/`: Основные модули приложения (авторизация, домашний экран, галерея).
- `MTCpath/`: Служебная папка с историей разработки, планами и отчетами.
